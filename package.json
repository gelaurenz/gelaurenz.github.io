{
  "name": "gelaurenz-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:assets": "node ./scripts/sync-assets.mjs",
    "dev": "npm run sync:assets && astro dev",
    "start": "npm run sync:assets && astro dev",
    "build": "npm run sync:assets && astro build",
    "preview": "npm run sync:assets && astro preview",
    "check": "npm run sync:assets && astro check"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
